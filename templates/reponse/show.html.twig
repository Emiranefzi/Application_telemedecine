{% extends 'base.html.twig' %}

{% block title %}Reponses{% endblock %}

{% block body %}
    <br><h1 style="margin-left:15cm; font-weight:bold;">Réponses</h1>
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reponse.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ reponse.titre }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ reponse.contenu }}</td>
            </tr>
        </tbody>
    </table>

  <a style="margin-left:14cm;"href="{{ path('app_reponse_index') }}">Retour à la liste</a>

    &nbsp; &nbsp; <a href="{{ path('app_reponse_edit', {'id': reponse.id}) }}">Modifier la réponse</a>

    {{ include('reponse/_delete_form.html.twig') }}
{% endblock %}
