
{{ form_start(form) }}
    {{ form_widget(form) }}
    <button style="margin-left:16cm; font-size:18px;" class="btn btn-secondary my-2 my-sm-0">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}