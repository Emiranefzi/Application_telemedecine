<form method="post" action="{{ path('app_generaliste_delete', {'id': generaliste.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ generaliste.id) }}">
    <br><button style="margin-left:16cm; font-size:18px;" class="btn btn-secondary my-2 my-sm-0">Supprimer</button>
</form>
